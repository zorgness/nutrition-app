{% extends 'base.html.twig' %}

{% block title %}Admin Modification Type{% endblock %}

{% block header %}{{ food.name ?  food.name|title : 'New food'}}  {% if food.imageName %}<img src="{{ asset('images/food/' ~ food.imageName)}}" class="avatar">{% endif %}{% endblock %}

{% block body %}
<div class="form-container m-4">

    {{form_start(form)}}
      <div>{{form_row(form.label, {'attr':{'class': 'tex-danger'}})}}</div>
      <div>{{form_row(form.image)}}</div>
      {{form_widget(form)}}
      <input type="submit" class="btn btn-primary" value="Submit"/>
    {{form_end(form)}}

</div>



{% endblock %}
